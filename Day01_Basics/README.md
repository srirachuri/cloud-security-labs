# 🐧 Day 01 — Bash & Navigation Basics

## 🎯 Goal
Understand basic Linux navigation commands and get comfortable using the terminal.

---

## 📌 Commands Used

### 🔹 Check current directory
```bash
pwd
🔹 List files
ls
ls -l
ls -a
ls → list files
ls -l → detailed view (permissions, owner, size)
ls -a → shows hidden files
🔹 Change directory
cd folder_name
cd ..
cd ~
cd .. → go back
cd ~ → home directory
